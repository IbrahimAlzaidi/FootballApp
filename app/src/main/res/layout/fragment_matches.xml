<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.example.footballapp.ui.leagueDetails.matches.MatchesViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.leagueDetails.matches.MatchesFragment">
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/matchScheduledRecycler"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="@dimen/margin2X"
            android:orientation="vertical"
            app:items="@{viewModel.matchesScheduled.toData().schedulerMatchInfo}"
            android:layout_marginHorizontal="@dimen/margin1X"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:listitem="@layout/match_scheduld_item"
            />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
