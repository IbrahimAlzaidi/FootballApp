<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="item"
            type="com.example.footballapp.model.response.base.baseLeague.LeagueInfo" />
        <variable
            name="listener"
            type="com.example.footballapp.ui.home.LeagueInteractionListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/image_League"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_marginHorizontal="@dimen/margin4X"
            android:scaleType="centerInside"
            app:ImageUrl="@{item.league.logo}"
            android:onClick="@{()-> listener.onLeagueSelected(item.league.id)}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.App.MediumComponent"
            tools:src="@tools:sample/avatars" />

        <TextView
            android:id="@+id/text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@{item.league.name}"
            android:textSize="@dimen/size2X"
            android:textStyle="bold"
            android:lines="1"
            android:layout_marginTop="@dimen/size0X"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@id/image_League"
            app:layout_constraintStart_toStartOf="@id/image_League"
            app:layout_constraintTop_toBottomOf="@id/image_League"
            tools:text="Europa League" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
