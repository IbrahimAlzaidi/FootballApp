<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.example.footballapp.ui.leagueDetails.scorers.ScorersViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.leagueDetails.scorers.ScorersFragment">

        <View
            android:id="@+id/view_goals"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginHorizontal="@dimen/margin0X"
            android:layout_marginTop="@dimen/margin0X"
            android:background="@drawable/club_layout"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:showWhenSuccess="@{viewModel.topScorers}" />

        <TextView
            android:id="@+id/player_text_goal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/margin3X"
            android:text="@string/player"
            android:textColor="@color/black"
            android:textSize="@dimen/size4X"
            app:layout_constraintBottom_toBottomOf="@id/view_goals"
            app:layout_constraintStart_toStartOf="@id/view_goals"
            app:layout_constraintTop_toTopOf="@id/view_goals"
            app:showWhenSuccess="@{viewModel.topScorers}" />

        <TextView
            android:id="@+id/goals_text"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_marginEnd="@dimen/margin2X"
            android:gravity="center"
            android:text="@string/goals"
            android:textColor="@color/black"
            android:textSize="@dimen/size2X"
            app:layout_constraintBottom_toBottomOf="@id/view_goals"
            app:layout_constraintEnd_toStartOf="@id/assist_text"
            app:layout_constraintTop_toTopOf="@id/view_goals"
            app:showWhenSuccess="@{viewModel.topScorers}" />

        <TextView
            android:id="@+id/assist_text"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_marginEnd="@dimen/margin2X"
            android:gravity="center"
            android:text="@string/assist"
            android:textColor="@color/black"
            android:textSize="@dimen/size2X"
            app:layout_constraintBottom_toBottomOf="@id/view_goals"
            app:layout_constraintEnd_toEndOf="@id/view_goals"
            app:layout_constraintTop_toTopOf="@id/view_goals"
            app:showWhenSuccess="@{viewModel.topScorers}" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/top_Scorers_goals"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginHorizontal="@dimen/margin0X"
            android:orientation="vertical"
            app:items="@{viewModel.topScorers.toData().topScorersInfo}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/view_goals"
            app:showWhenSuccess="@{viewModel.topScorers}"
            tools:listitem="@layout/top_score_item" />

        <ProgressBar
            android:id="@+id/loading_progress"
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:showWhenLoading="@{viewModel.topScorers}" />

        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/lottie_error"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:lottie_autoPlay="true"
            app:lottie_rawRes="@raw/no_connection"
            app:showWhenError="@{viewModel.topScorers}" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
